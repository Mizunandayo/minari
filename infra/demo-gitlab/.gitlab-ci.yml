stages: [test]

pytest:
  stage: test
  image: python:3.12-slim
  script:
    - pip install pytest
    - pytest tests/ --junitxml=report.xml -p no:cacheprovider || true
  artifacts:
    when: always
    reports:
      junit: report.xml
    paths: [report.xml]
