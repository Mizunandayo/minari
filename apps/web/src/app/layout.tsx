import type { Metadata } from "next";
import { Poppins } from "next/font/google";
import "./globals.css";

const poppins = Poppins({
  subsets: ["latin"],
  weight: ["400", "500", "600", "700"],
  variable: "--font-poppins",
  display: "swap",
});

export const metadata: Metadata = {
  title: "Minari — Autonomous Flaky Test Intelligence",
  description:
    "Minari detects, diagnoses, fixes, verifies, and delivers flaky test repairs via GitLab MCP — in under two minutes.",
  robots: { index: false }, // demo app, not for indexing
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en" className={poppins.variable}>
      <body>{children}</body>
    </html>
  );
}
