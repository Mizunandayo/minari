from __future__ import annotations

from app.agents.state import MinariState


async def merger_node(state: MinariState) -> MinariState:
    # Day 5: create MR via GitLab MCP with templated description.
    return state
